using UnityEngine;

public class PlayerPickup : MonoBehaviour
{
    private void Update()
    {/*
        if (Input.GetKeyDown(KeyCode.E))
        {
            Physics.Raycast();
        }*/
    }
}
